   <div class="col-md-3 col-sm-3 hidden-xs fill">
      <div class="left-nav fill">
      <p>Welcome, <%= @current_user.username %></p>
      <p><%= link_to "Home", dashboard_path %></p>
      <p><%= link_to "Add Classroom", new_user_classgroup_path(user_id: @current_user) %></p>
      <hr>
      <p>My Classes</p>
      <% @classes.each do |c| %>
         <p><%= link_to c.name, user_classgroup_path(user_id: @current_user, id: c) %></p>
      <% end %>
      <hr>
      <%= link_to "Sign Out", sign_out_path %>
   </div>
</div>
